(function(){
'use strict';
const TYPES=[['sparse_most_likely_diagnosis','Sparse presentation: most likely diagnosis'],['close_mimic_discrimination','Close-mimic discrimination'],['first_line_investigation','First-line investigation'],['dangerous_diagnosis_priority_exclusion','Dangerous diagnosis: priority exclusion'],['next_step_after_initial_result','Next step after an initial result'],['immediate_emergency_management','Immediate emergency management'],['stable_first_line_treatment','Standard first-line treatment'],['contraindication_caveat_switch','Contraindication or caveat switch'],['failure_or_deterioration','Failure or deterioration'],['escalation_referral_disposition','Escalation, referral or disposition']];
const STAGES=[{id:'generation',label:'Generate ten questions',percent:25},{id:'sparse',label:'Sparse-stem difficulty review',percent:40},{id:'options',label:'Option normalisation',percent:55},{id:'category',label:'Semantic answer-category review',percent:70},{id:'distractors',label:'Distractor-validity review',percent:84},{id:'source',label:'Uploaded-source fidelity review',percent:91,knowledgeOnly:true},{id:'shuffle',label:'Balanced A–E shuffling',percent:96,local:true},{id:'final',label:'Final validation and rendering',percent:100,local:true}];
function optionSchema(){return{type:'object',additionalProperties:false,required:['id','text','topicId','topicName','conditionId','conditionName','param'],properties:{id:{type:'string',enum:['A','B','C','D','E']},text:{type:'string'},topicId:{type:'string'},topicName:{type:'string'},conditionId:{type:'string'},conditionName:{type:'string'},param:{type:'string',enum:['investigations','treatment','escalation','mimics','redFlags']}}};}
function questionSchema(knowledge){const properties={id:{type:'string'},questionNumber:{type:'integer',minimum:1,maximum:10},questionType:{type:'string',enum:TYPES.map(item=>item[0])},questionTypeLabel:{type:'string',enum:TYPES.map(item=>item[1])},topicId:{type:'string'},topicName:{type:'string'},targetConditionId:{type:'string'},targetCondition:{type:'string'},learningPoint:{type:'string'},stem:{type:'string'},leadIn:{type:'string'},options:{type:'array',minItems:5,maxItems:5,items:optionSchema()},correctOptionId:{type:'string',enum:['A','B','C','D','E']},decisiveClue:{type:'string'},rationale:{type:'string'},strongestDistractorId:{type:'string',enum:['A','B','C','D','E']},strongestDistractorExplanation:{type:'string'},guideline:{type:'object',additionalProperties:false,required:['source','title','checkedDate','url'],properties:{source:{type:'string'},title:{type:'string'},checkedDate:{anyOf:[{type:'string'},{type:'null'}]},url:{anyOf:[{type:'string'},{type:'null'}]}}}};const required=Object.keys(properties);if(knowledge){properties.sourceSupport={type:'object',additionalProperties:false,required:['conceptId','sourceRefs','supportStatement'],properties:{conceptId:{type:'string'},sourceRefs:{type:'array',minItems:1,items:{type:'string'}},supportStatement:{type:'string'}}};required.push('sourceSupport');}return{type:'object',additionalProperties:false,required,properties};}
function quizSchema(knowledge){return{type:'object',additionalProperties:false,required:['schemaVersion','quizId','topic','generatedAt','difficulty','questions'],properties:{schemaVersion:{type:'string',enum:['ukmla-ai-quiz-v2']},quizId:{type:'string'},topic:{type:'string'},generatedAt:{type:'string'},difficulty:{type:'string',enum:['very_difficult']},questions:{type:'array',minItems:10,maxItems:10,items:questionSchema(knowledge)}}};}
function sourcePayload(conditions){return conditions.map(item=>({conditionId:item.id||item.conditionId,topicId:item.topicId,topicName:item.topic||item.topicName,name:item.name||item.targetCondition,profile:item.profile||'clinical',fields:item.fields,labels:item.labels,sourceRefs:item.sourceRefs||[]}));}
function typeOrder(types){return types.map((id,index)=>`${index+1}. ${id} — ${TYPES.find(item=>item[0]===id)?.[1]||id}`).join('\n');}
function generationPrompt(config){const source=config.knowledge?'uploaded study-pack concept map':'UKMLA card atlas';const guide=config.knowledge?`Use guideline source "Uploaded study material", title "${config.sourceTitle||config.topic}", and null checkedDate/url.`:'Use the supplied card fields as the factual boundary. Do not invent exact doses or thresholds absent from them. Use guideline source "Internal UKMLA card atlas" with null checkedDate/url.';return`Create exactly ten very difficult UKMLA-style single-best-answer questions from the ${source}.\n\nThe ten supplied targets are fixed. Question 1 must use targets[0], question 2 targets[1], and so on. Preserve every target ID, topic ID, name and topic. Use each exactly once and never substitute a more familiar target.\n\nQUESTION TYPES IN EXACT ORDER:\n${typeOrder(config.questionTypes)}\n\nUse sparse authentic stems with one subtle discriminator. Give five short, homogeneous, plausible options and one unambiguously best answer. Every option must contain accurate topic, condition and scoring-aspect metadata. Include a concise rationale and strongest-distractor explanation. ${guide}\n${config.knowledge?'Each question must include sourceSupport using the exact conceptId and one or more supplied sourceRefs. Do not add unsupported doses, thresholds, diagnoses or management claims.':''}\n\nSOURCE:\n${JSON.stringify({mode:config.knowledge?'knowledge_dump':'coverage_scheduler',sourceTitle:config.sourceTitle||config.topic,targets:sourcePayload(config.conditions)})}`;}
function checkpointPrompt(stage,config){const instructions={sparse:'Remove giveaway labels, excessive history and repeated clues. Keep only the subtle discriminator needed for a fair, very difficult UKMLA decision.',options:'Make all five options the same semantic answer category, similarly concise and grammatically compatible with the lead-in. Remove explanatory giveaway detail.',category:'Check that each correct answer directly answers the lead-in and every distractor belongs to the same clinically valid category and pathway stage.',distractors:'Make all four wrong options credible same-category competitors. Remove absurd, duplicate, overlong or trivially excluded options.',source:'Strictly verify every answer, clue, rationale and factual claim against the supplied concept fields and source references. Remove unsupported facts.'};return`Return the complete ten-question set in the same JSON schema. Preserve fixed target order, target IDs, topic IDs and question-type order.\n\nROUTINE CHECKPOINT: ${instructions[stage]}\n\nSOURCE TARGETS:\n${JSON.stringify(sourcePayload(config.conditions))}\n\nCURRENT SET:\n${JSON.stringify(config.currentSet)}`;}
function requestBody(prompt,knowledge,name){return{model:'gpt-5-mini',input:[{role:'system',content:[{type:'input_text',text:'Return only schema-conforming JSON.'}]},{role:'user',content:[{type:'input_text',text:prompt}]}],text:{format:{type:'json_schema',name,strict:true,schema:quizSchema(knowledge)}}};}
function outputText(data){if(typeof data?.output_text==='string')return data.output_text;for(const item of data?.output||[])for(const content of item.content||[])if(content?.type==='output_text'&&typeof content.text==='string')return content.text;return'';}
function validate(set,config){const errors=[];if(!set||!Array.isArray(set.questions)||set.questions.length!==10)errors.push('Exactly ten questions are required.');const types=new Set(),targets=new Set();(set?.questions||[]).forEach((question,index)=>{const expected=config.conditions[index];const expectedId=expected.id||expected.conditionId;types.add(question.questionType);targets.add(question.targetConditionId);if(question.questionNumber!==index+1)errors.push(`Q${index+1}: number changed.`);if(question.questionType!==config.questionTypes[index])errors.push(`Q${index+1}: type changed.`);if(question.targetConditionId!==expectedId)errors.push(`Q${index+1}: target changed.`);if(question.topicId!==expected.topicId)errors.push(`Q${index+1}: topic changed.`);if(!Array.isArray(question.options)||question.options.length!==5)errors.push(`Q${index+1}: five options required.`);if((question.options||[]).map(option=>option.id).join('')!=='ABCDE')errors.push(`Q${index+1}: options must be A–E.`);if(!(question.options||[]).some(option=>option.id===question.correctOptionId))errors.push(`Q${index+1}: invalid answer key.`);if(config.knowledge&&(!question.sourceSupport?.conceptId||!question.sourceSupport?.sourceRefs?.length))errors.push(`Q${index+1}: source support missing.`);});if(types.size!==10)errors.push('Question types are not unique.');if(targets.size!==10)errors.push('Targets are not unique.');return errors;}
function balancedShuffle(set){const letters=['A','B','C','D','E'],targets=[];while(targets.length<set.questions.length)targets.push(...letters);targets.length=set.questions.length;for(let i=targets.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[targets[i],targets[j]]=[targets[j],targets[i]];}set.questions.forEach((question,index)=>{const oldStrong=question.options.find(option=>option.id===question.strongestDistractorId);const correct=question.options.find(option=>option.id===question.correctOptionId);const wrong=question.options.filter(option=>option!==correct);for(let i=wrong.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[wrong[i],wrong[j]]=[wrong[j],wrong[i]];}let w=0;question.options=letters.map(letter=>({...((letter===targets[index])?correct:wrong[w++]),id:letter}));question.correctOptionId=targets[index];question.strongestDistractorId=question.options.find(option=>oldStrong&&option.text===oldStrong.text)?.id||question.options.find(option=>option.id!==question.correctOptionId).id;});return set;}
window.UKMLA_V2_AI_SCHEMA={TYPES,STAGES,quizSchema,requestBody,outputText,generationPrompt,checkpointPrompt,validate,balancedShuffle};
})();
