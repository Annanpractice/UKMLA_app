(function(){'use strict';const nativeFetch=window.fetch.bind(window);const API='https://api.openai.com/v1/responses';function emit(message,detail){document.dispatchEvent(new CustomEvent('ukmlaAiGenerationCheckpoint',{detail:{message,detail:detail||null}}))}function labelFor(body){try{const x=JSON.parse(body||'{}');return x?.text?.format?.name==='ukmla_option_normalisation'?'normalisation':'quiz'}catch{return'quiz'}}function questionProgress(text){const nums=[...text.matchAll(/"questionNumber"\s*:\s*(\d+)/g)].map(m=>Number(m[1]));const types=[...text.matchAll(/"questionTypeLabel"\s*:\s*"([^"]+)"/g)].map(m=>m[1]);if(!nums.length)return null;const n=Math.max(...nums);return types[n-1]?`Receiving question ${n} of 10: ${types[n-1]}…`:`Receiving question ${n} of 10…`}async function streamRequest(input,init){let body;try{body=JSON.parse(init.body)}catch{return nativeFetch(input,init)}const mode=labelFor(init.body);body.stream=true;emit(mode==='quiz'?'Submitting the structured UKMLA quiz request to OpenAI…':'Submitting the option-normalisation request to OpenAI…');const response=await nativeFetch(input,{...init,body:JSON.stringify(body)});if(!response.ok||!response.body)return response;const type=response.headers.get('content-type')||'';if(!type.includes('text/event-stream'))return response;const reader=response.body.getReader(),decoder=new TextDecoder();let buffer='',output='',finalEvent=null,lastQuestion=0,lastChars=0;while(true){const part=await reader.read();if(part.done)break;buffer+=decoder.decode(part.value,{stream:true});const chunks=buffer.split('\n\n');buffer=chunks.pop()||'';for(const chunk of chunks){const dataLines=chunk.split('\n').filter(l=>l.startsWith('data:')).map(l=>l.slice(5).trim());for(const line of dataLines){if(!line||line==='[DONE]')continue;let event;try{event=JSON.parse(line)}catch{continue}if(event.type==='response.created')emit(mode==='quiz'?'OpenAI accepted the quiz request.':'OpenAI accepted the normalisation request.');if(event.type==='response.in_progress')emit(mode==='quiz'?'OpenAI is generating the structured quiz output.':'OpenAI is generating normalised answer options.');if(event.type==='response.output_text.delta'){output+=event.delta||'';if(mode==='quiz'){const msg=questionProgress(output);const m=msg&&Number((msg.match(/question (\d+)/)||[])[1]);if(msg&&m>lastQuestion){lastQuestion=m;emit(msg)}else if(output.length-lastChars>2500){lastChars=output.length;emit(`Receiving structured quiz output… ${output.length.toLocaleString()} characters received.`)}}else if(output.length-lastChars>800){lastChars=output.length;emit(`Receiving normalised option text… ${output.length.toLocaleString()} characters received.`)}}if(event.type==='response.output_text.done'&&event.text)output=event.text;if(event.type==='response.completed'){finalEvent=event;emit(mode==='quiz'?'OpenAI completed the structured quiz response.':'OpenAI completed the option-normalisation response.')}if(event.type==='response.failed'||event.type==='error')emit('OpenAI reported an error while generating the response.')}}}const responseObject=finalEvent?.response||{output_text:output,output:[{content:[{type:'output_text',text:output}]}]};if(!responseObject.output_text)responseObject.output_text=output;return new Response(JSON.stringify(responseObject),{status:200,headers:{'Content-Type':'application/json'}})}window.fetch=function(input,init){const url=typeof input==='string'?input:input&&input.url;if(url===API&&init&&init.method==='POST')return streamRequest(input,init);return nativeFetch(input,init)}})();