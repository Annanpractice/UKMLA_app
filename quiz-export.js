(function(){'use strict';const KEY='ukmlaAiGeneratedQuizSetsV1';const clean=v=>String(v||'').replace(/\s+/g,' ').trim();function latest(){try{const x=JSON.parse(localStorage.getItem(KEY)||'[]');return Array.isArray(x)?x[0]:null}catch{return null}}function text(){const s=latest();if(s&&Array.isArray(s.questions)){const out=['UKMLA AI Quiz','Topic: '+clean(s.topic),''];s.questions.forEach((q,i)=>{out.push((i+1)+'. '+clean(q.stem));if(q.leadIn)out.push(clean(q.leadIn));(q.options||[]).forEach(o=>out.push(o.id+'. '+clean(o.text)));out.push('Answer: '+clean(q.correctOptionId));if(q.rationale)out.push('Rationale: '+clean(q.rationale));out.push('')});return out.join('\n')}const e=document.querySelector('.quiz-focus #ai-generated-quiz,.quiz-focus #quiz-panel,.quiz-focus .quiz-panel,#ai-generated-quiz,#quiz-panel,.quiz-panel');return e?clean(e.innerText):''}function toast(m){let t=document.querySelector('.export-toast');if(!t){t=document.createElement('div');t.className='export-toast';document.body.appendChild(t)}t.textContent=m;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1800)}document.addEventListener('ukmlaExportQuiz',async()=>{const value=text();if(!value)return toast('No quiz available.');try{await navigator.clipboard.writeText(value);toast('Quiz copied as text.')}catch{toast('Copy unavailable in this browser.')}})})();