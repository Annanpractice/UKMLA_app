name: Apply durable generated-set patch

on:
  push:
    branches:
      - "agent/durable-generated-question-storage"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/durable-generated-question-storage
          fetch-depth: 0
      - name: Apply durable generated-set patch
        run: python scripts/apply_durable_generated_set_patch.py
      - name: Commit patched files
        run: |
          if git diff --quiet; then
            echo "Patch already applied"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add v2/ai-engine.js v2/ai-save-recovery.js v2/app.html service-worker.js scripts/validate_question_bank.js scripts/validate_recency_background.js scripts/validate_durable_generated_sets.js .github/workflows/pages.yml .github/workflows/durable-generated-set.yml
          git commit -m "Keep completed generated sets until verified storage"
          git push origin HEAD:agent/durable-generated-question-storage
